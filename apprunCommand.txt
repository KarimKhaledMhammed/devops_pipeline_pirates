minikube start

minikube tunnel

kubectl port-forward svc/argocd-server -n argocd 8080:443

kubectl port-forward svc/pirate-monitor-grafana -n monitoring 3000:80

Your App: https://pirates.local (or the IP from the tunnel)

    Try logging in—your user data should still be there thanks to the persistence fix!

    kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d; echo

ArgoCD: https://localhost:8080

    Check if all "Hearts" are green.

Grafana: http://localhost:3000

    Check if your dashboards are showing data.





kubectl exec -it pipeline-pirates-pirates-db-0 -n pipeline-pirates -- psql -U postgres -d pirates_db

SELECT * FROM users;



CREATE TABLE IF NOT EXISTS users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(255) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL
);